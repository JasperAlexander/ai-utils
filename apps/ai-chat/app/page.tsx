'use client'

import styles from './page.module.css'

export default function IndexPage() {
  return (
    <div className={styles.indexPage}>
      <h2 className={styles.indexPageTitle}>Create or open a folder or chat</h2>
    </div>
  )
}
